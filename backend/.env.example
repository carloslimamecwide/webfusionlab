# Variaveis de Ambiente

# Servidor
PORT=3001
NODE_ENV=development

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=webfusionlab

# JWT Secret
JWT_SECRET=seu_secret_jwt_aqui_mude_em_producao

# Email - Gmail (configure com suas credenciais)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu_email@gmail.com
SMTP_PASSWORD=sua_senha_app

# Email - Servidor de Teste (Mailtrap, Gmail, etc)
# Para testar localmente sem SMTP real, use Ethereal Email (temporario)
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_USER=seu_email_do_ethereal@ethereal.email
# SMTP_PASSWORD=sua_senha_do_ethereal

# Email padrao para envios
SENDER_EMAIL=seu_email@gmail.com
SENDER_NAME=WebFusionLab

# Email do administrador (recebe contactos do formulario)
CONTACT_ADMIN_EMAIL=seu_email@gmail.com
